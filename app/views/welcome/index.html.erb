<!--<div class="display-flex">
  <% if user_signed_in? %>
    <h1>Hello, <%= current_user.username %> !</h1>
    <%= link_to 'Profile', url_for(root_url+current_user.username), :class => 'navbar-link' %> |
    <%= link_to "Log out", logout_path, method: :delete, :class => 'navbar-link' %>
  <% else %>
  <h1>Welcome on Rtwitter !</h1>
    <div class="title">
      <h1 class="text-end">Ça se passe maintenant</h1>
    </div>
    <div class="loginsign">
      <%= link_to "Sign up", new_user_registration_path, :class => 'navbar-link' %> |
      <%= link_to "Login", new_user_session_path %>
    </div>
    <div class="photoacceuil">
      <%= image_tag("acceuiltwitter.png", alt: "fond acceuil") %>
    </div>
  <% end %>
</div>-->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/axentix@2.0.1/dist/axentix.min.css">
<div class="display-flex">
  <div class="block1">
    <button class="btn shadow-1 rounded-1 blue">Normal button</button>

  </div>
  <div class="block2">
    <div class="btn rounded-1 blue btn-press">Normal button</div>
  </div>

</div>